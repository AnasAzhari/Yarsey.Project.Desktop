<UserControl x:Class="Yarsey.Desktop.WPF.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Yarsey.Desktop.WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <GroupBox Margin="2,2,2,2" Header="Mode" Height="300" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                    <RadioButton Content="Light Mode" IsChecked="{Binding LightMode}" Margin="2,6,2,2"/>
                    <RadioButton Content="Dark Mode" Margin="2,6,2,2"/>
                </StackPanel>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
